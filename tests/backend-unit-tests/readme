# This file is used in api-spec.js to upload to a anipper.  It must start with a # to pass the test.

We are using mocha with the expect or should syntax.

Expect syntax:
https://github.com/Automattic/expect.js

or you can use the should syntax (used in the api tests):
http://unitjs.com/guide/should-js.html

Already committed 2 changes to develop branch (that weren’t committed to hot fix nor master) and are now committing to the hot fix.
Now committing 2nd to hot fix.  no commits to develop nor master