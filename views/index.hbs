<div class="container" role="main" ng-hide="showMainApp">
    <div class="jumbotron">
        <h1>Super Snippet Search</h1>
        <p>Super Snippet Search</p>
        <div growl inline="true" reference="generalMessages"></div>
        <div growl></div>
        <div class="widget">
            <form name="login-form" id="login-form" method="post" action="users/login">
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" required />
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" required />
                    <label for="rememberMe">Remember me</label>
                    <input type="checkbox" id="rememberMe" name="rememberMe" />
                </div>
                <div class="form-submit">
                    <input id="submit-login" class="btn btn-primary" type="submit" value="Log in" />
                        or <a id="createUserBtn" href="/users/create">Create an account</a>
                    {{#if error}}
                        <div id=loginError style="color:red;">{{error}}</div>
                    {{/if}}
                </div>
            </form>
        </div>
    </div>
</div>